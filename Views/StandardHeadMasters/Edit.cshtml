@model e_Office.Models.StandardHeadMaster

@{
    ViewBag.Title = "Edit";
}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title m-b-0">Edit Standard Head</h5>
            </div>
            <div class="card-body">

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-horizontal">
                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                        @Html.HiddenFor(model => model.StdHeadId)
                        <div class="form-group">
                            @Html.Label("Standard Head Name", htmlAttributes: new { @class = "control-label col-md-2" })
                            <div class="col-md-10">
                                @Html.EditorFor(model => model.StdHeadName, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.StdHeadName, "", new { @class = "text-danger" })
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-offset-2 col-md-10">
                                <input type="submit" value="Save" class="btn btn-default" />
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>
<div>
    @Html.ActionLink("Back to List", "Index")
</div>

<script src="~/Content/theme/assets/libs/jquery/dist/jquery.min.js"></script>
<script>
    jQuery('#StdHeadName').keyup(function () {
        var caps = jQuery('#StdHeadName').val();
        caps = caps.charAt(0).toUpperCase() + caps.slice(1);
        jQuery('#StdHeadName').val(caps);
    });
</script>
