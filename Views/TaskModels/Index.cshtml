@model IEnumerable<e_Office.Models.TaskModel>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title m-b-0 float-left">To-Do List</h5>

                <a class="btn btn-cyan float-right" href="@Url.Action("Create")">Create New</a>
            </div>
            <div class="card-body">
                @if (Model.Count() > 0)
                {
                    <table class="table table-bordered" style="width:98%;margin:10px;">
                        <thead class="thead-dark">
                            <tr class="text-center">
                                <th>
                                  Task Summary
                                </th>
                                <th>
                                 Start Date
                                </th>
                                <th>
                                 Due Date
                                </th>
                                <th>
                                  Priority
                                </th>
                                <th>
                                  Assigned To
                                </th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.TaskSummary)
                                    </td>
                                    <td>
                                        @item.StartDate.Value.ToLongDateString()
                                    </td>
                                    <td>
                                        @item.DueDate.Value.ToLongDateString()
                                    </td>
                                    <td>
                                        @Enum.GetName(typeof(e_Office.Models.EnumModel.Priority), Convert.ToInt32(item.TaskPriority))
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.AssignedTo)
                                    </td>
                                    <td class="text-center">
                                        <a href="@Url.Action("Edit", new { id = item.TaskId })" class="btn btn-sm btn-success">Edit</a>|
                                        <a href="@Url.Action("Delete", new { id = item.TaskId })" class="btn btn-sm btn-danger">Delete</a>
                                        @*@Html.ActionLink("Edit", "Edit", new { id = item.TaskId }) |
                                        @Html.ActionLink("Details", "Details", new { id = item.TaskId }) |
                                        @Html.ActionLink("Delete", "Delete", new { id = item.TaskId })*@
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
            </div>

        </div>
    </div>
</div>